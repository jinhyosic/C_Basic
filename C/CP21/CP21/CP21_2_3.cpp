/*
#include<stdio.h>
#include<string.h>

int main() {

	char one = 0;
	char two = 0;

	printf("이름\n");
	char str1[30] = {};
	fgets(str1, sizeof(str1 - 1), stdin);

	printf("나이\n");
	char str2[10] = {};
	fgets(str2, sizeof(str2 - 1), stdin);

	printf("%s", one);

	printf("이름\n");
	char str3[30] = {};
	fgets(str3, sizeof(str3 - 1), stdin);

	printf("나이\n");
	char str4[10] = {};
	fgets(str4, sizeof(str4 - 1), stdin);

	one = strcat(str1, str2);
	two = strcat(str3, str4);

	printf("%s", one);
	printf("%s", two);



	return 0;
}
*/